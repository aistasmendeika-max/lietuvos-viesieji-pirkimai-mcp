[project]
name = "lietuvos-viesieji-pirkimai-mcp"
version = "0.1.0"
description = "MCP serveris Lietuvos viešųjų pirkimų duomenims"
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.12.0",
  "httpx>=0.27.0",
  "python-dotenv>=1.0.1",
]

[project.scripts]
lietuvos-viesieji-pirkimai-mcp = "server:main"
